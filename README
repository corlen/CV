Repositorio com o meu curriculum em Tex.



Global setup:
 Download and install Git
  git config --global user.name "Max"
  git config --global user.email maximiliano.santiago@gmail.com
        
Next steps:
  mkdir CV
  cd CV
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:corlen/CV.git
  git push origin master

Transformar em pdf : pdflatex [arquivo]
